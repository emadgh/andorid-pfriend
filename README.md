# PFriend

PFriend is a native Android social daily intake/output tracker with a lightweight PHP + MySQL API.

Development branches:

- `android-native` — Kotlin + Jetpack Compose Android application
- `php-api` — PHP REST API, installer and read-only statistics page

> The repository intentionally keeps the Android client and PHP backend on separate branches.
